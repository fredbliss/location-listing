import { ListingFiltersPipe } from './listing-filters.pipe';

describe('ListingFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new ListingFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
